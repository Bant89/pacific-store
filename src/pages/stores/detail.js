import React from "react"
import useStore from "hooks/useStore"
import SEO from "components/seo"
import { Layout } from "components/Layout"

export default function StoreDetail() {

    return (
        <Layout>
            <SEO title="Store Detail" />
        </Layout>
    )
}